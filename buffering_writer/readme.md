### Сборка и запуск (Linux)
из текущего каталога:
```
mkdir build && cd build
cmake ..
cmake --build .
./buffering_writer
```

TODO: пока написал только примитивный файл, выводящий в `out.txt` по 10 сообщений из 5 различных потоков, в ближайшее время добавлю unit-тесты
